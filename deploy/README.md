# Deployment Artifacts

These files are preparation-only artifacts for the always-on server. They are designed for an isolated dry-run start and do not modify nginx, TLS, DNS, or existing services by themselves.

Use `docs/deployment-plan.md` as the source of truth before copying anything to the server.

