# Nginx Placeholder

No nginx configuration is included yet.

Reason: `secretary.vacanator.xyz` has a REG.RU A record but public DNS resolution is not confirmed. Until the user separately confirms DNS and approves a public endpoint, the service must stay local-only on `127.0.0.1:18097`, with TLS and Telegram webhooks disabled.

